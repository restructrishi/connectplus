# Database (use your actual URL for local dev)
DATABASE_URL=postgresql://crm_user:StrongPassword2@172.16.110.46:5432/cachedigitech_crm
SEED_DATABASE_URL=postgresql://postgres:1234@localhost:5432/cachedigitech_crm
# Optional: use a different DB only for seeding (e.g. local PostgreSQL when 172.16.x.x is unreachable)
# SEED_DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/cachedigitech_crm

# Optional: use a superuser URL only for running migrations (when crm_user has "permission denied for schema public")
# MIGRATE_DATABASE_URL=postgresql://postgres:YOUR_POSTGRES_PASSWORD@172.16.110.46:5432/cachedigitech_crm

# JWT
JWT_SECRET="your-super-secret-jwt-key-min-32-chars"
JWT_EXPIRES_IN="7d"

# Server
NODE_ENV=development
PORT=4000
API_BASE_URL=https://api.cachedigitech.com

# HRMS (read-only integration)
HRMS_SERVICE_URL=http://localhost:4001
HRMS_API_KEY=optional-internal-key

# Rate limit (requests per 15 min per IP)
RATE_LIMIT_MAX=200
